# Environment Summary

> ⚠️ **Security notice — do not commit real secrets.**
> This file previously contained a plaintext value for `DB_PASSWORD` (table row, "Required environment variables" section) and a related `DATABASE_URL` example. **If either value was ever pushed to version control, rotate the database password immediately.** All secret values must be replaced with placeholders (e.g. `<REDACTED>`) before committing. No real credentials should appear in this file.

Generated by `/env-validation` — 2026-05-16

---

## Service status

| Service  | URL                      | Status          |
|----------|--------------------------|-----------------|
| Frontend | http://localhost:3000    | ✅ HTTP 200     |
| Backend  | http://localhost:3010    | ✅ HTTP 200     |
| Database | PostgreSQL on port 5432  | ✅ Running (Docker) |

---

## Frontend base URL

```
http://localhost:3000
```

- Start command: `cd frontend && ./node_modules/.bin/react-scripts start`
- Framework: Create React App (react-scripts 5)
- Position page URL: `http://localhost:3000/positions/1`

---

## Backend base URL

```
http://localhost:3010
```

- Start command: `cd backend && ./node_modules/.bin/ts-node-dev --respawn --transpile-only src/index.ts`
- Framework: Express + Prisma
- API confirmed working:
  - `GET /positions` → 200 (returns 2 positions)
  - `GET /positions/1/interviewFlow` → 200 (returns interview steps)
  - `GET /positions/1/candidates` → 200 (returns candidates)

---

## Database

- Start command: `docker compose up -d db` (from project root)
- Engine: PostgreSQL 
- Config: from root `.env` file

---

## Required environment variables

| Variable       | Source             | Value                                              | Purpose                |
|----------------|--------------------|----------------------------------------------------|------------------------|
| `DB_PASSWORD`  | `backend/.env`     | `<REDACTED>`                                       | Postgres password      |
| `DB_USER`      | `backend/.env`     | `LTIdbUser`                                        | Postgres user          |
| `DB_NAME`      | `backend/.env`     | `LTIdb`                                            | Postgres database name |
| `DB_PORT`      | `backend/.env`     | `5432`                                             | Postgres port          |
| `DATABASE_URL` | `backend/.env`     | `postgresql://LTIdbUser:<pw>@localhost:5432/LTIdb` | Prisma connection URL  |

No frontend-specific environment variables required (base URLs are hardcoded in source).

---

## Test data

Position under test: **ID 1** — "Senior Full-Stack Engineer"

Interview steps (phase columns):
| Step ID | Step Name           | Order |
|---------|---------------------|-------|
| 1       | Initial Screening   | 1     |
| 2       | Technical Interview | 2     |
| 3       | Manager Interview   | 2     |

Candidates in position 1:
| Candidate ID | Full Name      | Application ID | Current Step        | Score |
|--------------|----------------|----------------|---------------------|-------|
| 1            | John Doe       | 1              | Technical Interview | 5     |
| 2            | Jane Smith     | 3              | Technical Interview | 4     |
| 3            | Carlos García  | 4              | Initial Screening   | 0     |

> **Note:** The actual stage titles in the DB are `Initial Screening`, `Technical Interview`, `Manager Interview` — not the Spanish titles mentioned in the spec (`Aplicado`, `Entrevista`, etc.). Tests must use the actual DB values.

---

## Placeholders

None — all values confirmed from live services.

---

## Startup sequence

To reproduce the environment from scratch:

```bash
# 1. Start database
docker compose up -d db

# 2. Start backend
cd backend
npm install                          # if not already done
./node_modules/.bin/prisma migrate deploy
./node_modules/.bin/ts-node-dev --respawn --transpile-only src/index.ts &

# 3. Start frontend
cd ../frontend
npm install                          # if not already done
./node_modules/.bin/react-scripts start &

# 4. Verify
curl http://localhost:3010/positions/1/candidates
curl http://localhost:3000
```
