# Prompts - Franco Borgato

| # | Prompt | Respuesta (resumen) |
|---|--------|---------------------|
| 1 | Lee el README y la estructura del proyecto, luego crea los tests E2E con Playwright para: Escenario 1 (carga de la página Position) y Escenario 2 (cambio de fase de un candidato por drag & drop con validación del PUT /candidate/:id) | Se añadieron `data-testid` a los componentes React (PositionDetails, StageColumn, CandidateCard), se actualizó `playwright.config.ts` con `baseURL`, y se creó `frontend/tests/e2e/position.spec.ts` con ambos escenarios usando intercepción de requests Playwright para validar el PUT al backend. |
| 2 | El Escenario 2 falla con "element(s) not found" para la tarjeta de Carlos García en Initial Screening. | Se identificó una race condition en PositionDetails.js (fetchInterviewFlow y fetchCandidates corrían en paralelo sin coordinación). Se refactorizó con `Promise.all` y un solo `setStages`. También se actualizó `beforeEach` para esperar explícitamente a que aparezca al menos un `candidate-card-*` en el DOM. |
| 3 | El Escenario 1 falla: "element(s) not found" para Carlos García en phase-column-initial-screening. El snapshot muestra a Carlos en Technical Interview. | Causa: falta de aislamiento entre tests. Los 2 workers paralelos comparten la misma DB; el Escenario 2 movió a Carlos y el cambio persistió. Fix: mockear las respuestas GET en `beforeEach` con `page.route()` para que cada test parta de un estado fijo. El Escenario 2 también mockea el PUT (capturando la request para validarla) para no alterar la DB real. |
| 4 | El Escenario 2 falla: capturedPutUrl es "" y waitForResponse termina con "Test ended". El snapshot confirma que el drag sí se ejecutó. | Causa: timing. `capturedPutUrl` se verificaba inmediatamente tras `mouse.up()`, antes de que el fetch PUT (disparado por onDragEnd de React de forma asíncrona) llegara al route handler. Fix: mover todas las aserciones sobre capturedPutUrl/capturedPutBody a después del `await putResponsePromise`, que actúa como punto de sincronización garantizando que el handler ya fue ejecutado. |
