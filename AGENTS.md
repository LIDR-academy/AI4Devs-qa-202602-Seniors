# Architecture
- React frontend in `frontend/`.
- Node banckend in `backend/`.

# Documentation
- Detailed conventions with examples live id `docs/`.
- **Do NOT real all docs upfront.**
- When working on a task, use this map to find and read only the docs relevat to your task:

```
docs/
├── documentation-guidelines.md
├── backend/
│   └── api-documentation.md
├── database/
├── frontend/
│   ├── project-overview.md      # Tech stack, entry points, folder structure
│   ├── conventions.md           # Naming conventions, JS/TS language mix
│   ├── component-catalog.md     # All components: props, responsibility, usage
│   ├── patterns.md              # Architectural and design patterns
│   ├── styling.md               # CSS approach, Bootstrap usage, class naming
│   ├── services.md              # Service layer: API calls, data shapes
│   └── dependencies.md          # Key libraries and npm scripts
└── testing/
```